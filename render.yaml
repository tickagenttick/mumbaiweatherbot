services:
  - type: web
    name: mumbai-weather-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn weather_bot:app
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
